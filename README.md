# Scoop Software Installer

An interactive batch (`.bat`) script for installing and managing programs and buckets for the [Scoop](https://scoop.sh) package manager on Windows, through a simple terminal menu interface.
